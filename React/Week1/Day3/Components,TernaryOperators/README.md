# Ternary Operators , Lists in React  and CSS in React 
- We can simply add CSS to our react app by using the app.css module 
- We can convert the app.css to app.module.css and then we can use the classnames as objects `{classname }` inside the app.js 
- The `import './App.css';` statement imports the CSS from the location to out App


## Ternary Operators & Conditional Rendering
- We can use if else statements to render data or ui in the React app , but we can also use ternary operators which is more readable and short
- Check the React_App for better understanding 


## Using Lists in React
- We need to have a basic knowledge of arrays and arrya funtions in React