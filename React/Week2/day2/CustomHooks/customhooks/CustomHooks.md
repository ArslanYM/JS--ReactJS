# Using Custom hooks in React Apps
- What exactly are hooks ?
 - `Hooks` : A function that allows us to abstracts a lot of logic inside it. 
 -  Rules for creating hooks 
  1. Should start with `use`
  2. Should be declared at the highest level of component
  3. Should be inside a component 

- When creating hooks , remember that they return logical components not UI 
- 